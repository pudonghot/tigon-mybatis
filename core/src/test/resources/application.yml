# common config
mybatis:
  mapper-locations: classpath*:me/chyxion/tigon/mybatis/mapper/*Mapper.xml
  executor-type: REUSE

tigon:
  mybatis:
    quotation-mark: "`"
    insert-default-instead-null: false